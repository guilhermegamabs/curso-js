function valor (x, y) {
    

    if (x > y) {
        console.log(`${x} é maior que ${y}`);
    } else {
        console.log(`${y} é maior que ${x}`);
    }
}

valor(20,30);

/*
function max(x, y) {
    return x > y ? x : y;
}


const max2 = (x, y) => {
    return x > y ? x : y;
};

*/